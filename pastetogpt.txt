paste this whole text to chagpt nextsubscrtipopn prompt maker chat and make the 2 powerfull prtomps 
It's perfectly fine. Now, what we have to do is that tell it to analyze the whole backend and remove the unnecessary or unused or duplicate or inefficient code for a production website, inefficient in a way that we can make it more efficient and it should work as same as possible as same it is now. Each and everything right now in the backend folder is working, so tell it to use efficient methods that are industry level and remove any unnecessary code or any duplicate code and add comments all over the backend folder in each file. Okay, so my fellow developers can understand it. Then, rearrange the backend its subfolders to a industry level foldering method. Use best practices for rearranging the folders and the files. You can rearrange the names also, but t
hen you have to rearrange the imports, the exports, and in the code base also, if you do any change with that. And you have to do these all things. First of all, tell it to understand the whole backend, then do these all things and tell it to not do anything with the frontend, only the backend. Okay, do not do anything  with the frontend. Okay. and tell it to reduct the collections in the database like use it in a good methon and make it perfect make the best two powerfull prompts for these both for backend of nextsubscription. 
 